function[inputs] = get_composed_inputs(index, n_r, n_inputs, input_data)
    total_inputs = sum(n_inputs);
    
    % Data for iterations
    subs = zeros(1, total_inputs+1);
    dividors = ones(1, 1);
    adds = cat(2, 0, ones(1, total_states - 1));
    inputs = zeros(1, total_states);

    
end